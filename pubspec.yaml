name: spotify

version: 1.0.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  desktop_window: ^0.4.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/
